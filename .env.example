# ============================================================
# セットアップ:
#   1. npx supabase start
#   2. vercel env pull          → .env.local が生成される
#   3. npm run db:push          → スキーマ同期
#   4. npm run dev
#
# Vercel の Development 環境にローカル Supabase の値が設定済み
# vercel env pull --environment production で本番値を参照可能
# ============================================================

# Database (Vercel Supabase 連携: POSTGRES_URL)
POSTGRES_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"

# BetterAuth (Vercel に手動設定が必要)
BETTER_AUTH_SECRET="your-secret-here"

# Supabase Storage (Vercel Supabase 連携: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)
SUPABASE_URL="http://127.0.0.1:54321"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
